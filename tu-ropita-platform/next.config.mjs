/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', '1000logos.net', 'logos-world.net', 'upload.wikimedia.org'],
    },
  }

export default nextConfig;
